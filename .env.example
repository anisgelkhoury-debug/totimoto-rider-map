# Local-only. Do not commit real secret values.
VITE_GOOGLE_MAPS_API_KEY=
VITE_FIREBASE_VAPID_KEY=

# WeatherAPI.com key for local rider-weather testing.
# Embedded in the client bundle when set — do not treat as a server secret.
# Production should prefer a server proxy (WeatherAPI has no domain/referrer key locks).
VITE_WEATHERAPI_KEY=
