# Local-only. Do not commit real secret values.
VITE_GOOGLE_MAPS_API_KEY=
VITE_FIREBASE_VAPID_KEY=

# Optional. Open-Meteo commercial customer API key.
# Without it, the free api.open-meteo.com endpoint is used (non-commercial terms).
# VITE_OPEN_METEO_API_KEY=
